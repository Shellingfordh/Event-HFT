# Risk Controls

Paper mode is the default. Hard controls should include max daily loss, max open notional, max consecutive losses, stale event/data rejection, duplicate-event rejection, position reconciliation, acknowledgement timeout, cancellation failure handling and an emergency kill switch. Fail closed if risk state cannot be reconstructed.
