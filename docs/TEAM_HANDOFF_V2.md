# Team Handoff v2

Team-visible: C++ core, SPSC, timestamps, book, event normalization, features, risk, paper execution, tests and Rust parameter interface.

Private: source ranking, watchlists, exact classifier, alpha weights, impulse model, thresholds, timing, sizing, model weights and private datasets.

Every PR must state what changed, what was benchmarked, whether hot-path latency or memory allocation changed, whether risk changed, and whether secrets/proprietary alpha are exposed.
